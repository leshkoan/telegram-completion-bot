# Telegram-бот для дополнения фраз с помощью Ollama (для России)

Этот проект представляет собой Telegram-бота на Python, который использует локально запущенную нейросеть через Ollama для дополнения текста.



## 🚀 Быстрый старт



### 1. Выбор и установка модели



**Рекомендуется использовать модель `llama3:8b`**, так как она лучше следует инструкциям и дает более качественный и предсказуемый результат.



### 2. Предварительные требования

- Python 3.10+

- Docker (опционально, для запуска в контейнере)

- Установленный и запущенный [Ollama](https://ollama.ai/)

- Загруженная модель `llama3:8b`:

  ```bash

  ollama pull llama3:8b

  ```



### 3. Локальный запуск

1.  **Клонируйте репозиторий:**

    ```bash

    git clone <repository_url>

    cd telegram-completion-bot

    ```



2.  **Создайте и активируйте виртуальное окружение:**

    ```bash

    python -m venv venv

    source venv/bin/activate  # для Linux/macOS

    # или

    venv\Scripts\activate  # для Windows

    ```



3.  **Установите зависимости:**

    ```bash

    pip install -r requirements.txt

    ```



4.  **Настройте конфигурацию:**

    - Скопируйте файл `.env.example` в `.env`.

    - Убедитесь, что в `.env` указана модель `AI_MODEL=llama3:8b`.

    - Откройте `.env` и вставьте ваш токен Telegram-бота в переменную `BOT_TOKEN`.

    - При необходимости настройте прокси (см. ниже).



5.  **Запустите бота:**

    ```bash

    python main.py

    ```

## ⚙️ Настройка прокси (для обхода блокировок)

Все настройки находятся в файле `.env`.

1.  **Чтобы включить прокси**, измените значение `USE_PROXY`:

    ```
    USE_PROXY=true
    ```

2.  **Укажите адрес вашего прокси** в `PROXY_URL`.
    Поддерживаются форматы SOCKS5 и HTTP.

    **Примеры:**

    ```
    # SOCKS5 прокси
    PROXY_URL=socks5://proxy.example.com:1080

    # HTTP прокси
    PROXY_URL=http://proxy.example.com:8080

    # Прокси с авторизацией
    PROXY_URL=socks5://username:password@proxy.example.com:1080
    ```

Если `USE_PROXY` установлено в `false`, бот будет пытаться подключиться к Telegram API напрямую.

## 🎛️ Все переменные конфигурации (`.env`)

- `BOT_TOKEN`: Токен вашего Telegram-бота.
- `OLLAMA_HOST`: Адрес локального Ollama API.
- `AI_MODEL`: Название модели для генерации.
- `MAX_TOKENS`: Максимальное количество токенов в ответе.
- `TEMPERATURE`, `TOP_P`: Параметры генерации текста.
- `MAX_REQUESTS_PER_MINUTE`: Ограничение на количество запросов в минуту для одного пользователя.
- `LOG_LEVEL`: Уровень логирования (`INFO`, `DEBUG`, `WARNING`).
- `USE_PROXY`: `true` или `false`, включает использование прокси.
- `PROXY_URL`: Адрес прокси, если он используется.

## 🤖 Как пользоваться ботом

- `/start` - Приветственное сообщение.
- `/help` - Справка по командам и возможностям.
- Отправьте любую текстовую фразу, чтобы получить её продолжение.

## 🐳 Запуск с помощью Docker

1.  Убедитесь, что Ollama доступен с хост-машины (обычно `http://localhost:11434`).
2.  Заполните файл `.env`.
3.  Соберите и запустите Docker-контейнер:
    ```bash
    docker build -t telegram-completion-bot .
    # Для доступа к локальному Ollama используйте --network host
    docker run --rm -it --env-file .env --network host telegram-completion-bot
    ```

## 🏗️ Структура проекта

```
telegram-completion-bot/
├── main.py                     # Точка входа, инициализация бота
├── requirements.txt            # Зависимости Python
├── .env                        # Файл конфигурации (НЕ коммитить в Git)
├── .env.example                # Пример файла конфигурации
├── config.py                   # Загрузчик конфигурации
├── bot/
│   ├── handlers/               # Обработчики команд и сообщений
│   │   ├── start.py
│   │   └── completion.py
│   ├── services/               # Сервисы для работы с внешними API
│   │   └── ai_service.py
│   ├── middleware/             # Middleware и обработчики ошибок
│   │   └── error_handler.py
│   └── utils/                  # Вспомогательные функции
│       ├── availability_checker.py
│       └── validators.py
├── logs/
│   └── bot.log                 # Файл для логирования
├── Dockerfile                  # Docker-контейнер для приложения
└── README.md                   # Этот файл
```
